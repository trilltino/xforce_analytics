// Extractors removed - using middleware pattern instead
// UserCtx is injected via auth_middleware and extracted from request extensions


// No custom extractor needed - UserCtx comes from middleware
